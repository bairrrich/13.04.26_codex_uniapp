{
  "name": "@superapp/app",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"TODO: configure app lint\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@superapp/api": "workspace:*",
    "@superapp/ui": "workspace:*"
  }
}
